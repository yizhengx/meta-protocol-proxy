cc_library(
    name = "libbson",
    srcs = glob(["bson/*.c"]),
    hdrs = glob(["bson/*.h"]),
    includes = ["bson"],
    visibility = ["//visibility:public"],
    deps = [
        # Add any other dependencies that libbson might have
    ],
)
